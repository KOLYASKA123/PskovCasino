<UserControl x:Class="PskovCasino.MVVM.View.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PskovCasino.MVVM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0"  Orientation="Vertical">
            <Button 
                Content="Мой профиль"
                Command="{Binding NavigateProfileCommand}"
                />
            <Button 
                Content="Игровые сессии"
                Command="{Binding NavigateGameSessionsCommand}"
                />
        </StackPanel>
        <ContentControl Grid.Column="1" Content="{Binding Navigation.HomeCurrentView}">

        </ContentControl>
    </Grid>
</UserControl>
