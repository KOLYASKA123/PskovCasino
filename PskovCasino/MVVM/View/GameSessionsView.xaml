<UserControl x:Class="PskovCasino.MVVM.View.GameSessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PskovCasino.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding GameSessions}">
            <ListView.View>
                <GridView>
                    <!-- Здесь вы можете добавить столбцы для отображения свойств GameSession -->
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID}" />
                    <!-- Добавьте столбцы для других свойств, если необходимо -->
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
